/** How large an action button renders. */
export enum ButtonSize {
  Medium = "medium",
  Small = "small",
}

/** How much an action button stands out from the ones beside it. */
export enum ButtonVariant {
  Primary = "primary",
  Secondary = "secondary",
}
